package pl.polsl.tests;

import pl.polsl.model.Model;
import pl.polsl.classes.ArgValues;
import pl.polsl.classes.ErrorHandler;

//import org.junit.jupiter.api.*;
import static org.junit.jupiter.api.Assertions.*;




/**
 *
 * @author nimo
 */
public class Tests {
    
    // private Model model = new Model( new ArgValues("", "", false), new ErrorHandler() );
    
    //@BeforeEach
    void setUp() {
        //model.getResults().clear();
    }
    
    //@Test
    void testLoadFromCSV() {
        //assertNotNull(model.getData() );
        //assertFalse(model.getData().isEmpty() );
    }
}
